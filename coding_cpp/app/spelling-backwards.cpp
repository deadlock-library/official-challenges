#include "spelling-backwards.h"
#include <string>
#include <fstream>
#include <algorithm>

using namespace std;

string SpellingBackwards::read_backwards(const string line)
{
  string result(line);
  return result;
}
